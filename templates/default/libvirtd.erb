# Managed by Puppet! Local changes will be overridden!
#
# Environment settings for libvirtd service

# Arguments passed to libvirtd, add "-l" to listen on tcp
<%= @args_env_var %>="<%= @libvirtd_args.join(' ') %>"

# Additional environment settings
#
# Can include the following, for a full list see the libvirt and qemu
# documentation:
#  - KRB5_KTNAME: override Kerberos keytab for SASL/GSSAPI
#  - QEMU_AUDIO_DRV, SDL_AUDIODRIVER: override the QEMU/SDL default audio driver
#  - LIBVIRTD_NOFILES_LIMIT: override the limit on open files
#
<% @libvirtd_env.each_pair do |key, value| -%>
<%= key %>="<%= value %>"
<% end -%>
